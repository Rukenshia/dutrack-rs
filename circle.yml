machine:
  pre:
    - curl https://sh.rustup.rs -sSf > ./rustup-install.sh && chmod +x rustup-install.sh
    - ./rustup-install.sh -y
    - ~/.cargo/bin/rustup install nightly
    - ~/.cargo/bin/rustup default nightly


test:
  override:
    - cargo test